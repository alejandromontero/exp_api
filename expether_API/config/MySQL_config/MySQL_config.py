workload_mapping = {
        "id": "int",
        "name": "varchar(255)",
        "requirements": "varchar(255)",
        "active": "tinyint(1)"
        }

box_mapping = {
        "id": "varchar(255)",
        "hardware": "varchar(255)",
        "model": "varchar(255)",
        "quantity": "int"
        }
